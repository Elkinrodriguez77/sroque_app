# === Base de datos PostgreSQL ===
PGHOST=
PGPORT=5432
PGUSER=
PGPASSWORD=
PGDATABASE=
PGSCHEMA=prod
PGSSL=true

# === Servidor ===
PORT=3000

# === Sesiones (generar con: node -e "console.log(require('crypto').randomBytes(32).toString('base64url'))") ===
SESSION_SECRET=
